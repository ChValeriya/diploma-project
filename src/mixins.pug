mixin Menu
    ul.header__links 
        li.header__link
            a(href="#") Home
        li.header__link
            a(href="#") Product
        li.header__link
            a(href="#") Pricing
        li.header__link
            a(href="#") About 
        li.header__link
            a(href="#") Contact
            
mixin Features(img, title, subtitle)
    .features__card 
        .card__image !{val.img}
        .card__title= val.title
        .card__subtitle= val.subtitle